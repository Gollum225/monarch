package controller;

/**
 * Represents the types a rule can be.
 */
public enum RuleType {
    /**
     * The rule shows an indication that architecture is present.
     */
    MANDATORY,

    /**
     * Rank the quality of the repository.
     */
    QUALITY
}
