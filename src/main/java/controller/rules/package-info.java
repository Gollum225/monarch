/**
 * This package contains the concrete rules, which are applied to the {@link model.Repository}.
 * <p>
 * Every rule in this package should extend the {@link controller.Rule} class.
 */
package controller.rules;